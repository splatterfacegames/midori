[package]
name = "midori-ffi"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "C FFI bindings for the Midori procedural tree generator (engine plugins)"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
midori-core = { path = "../midori-core" }

[dev-dependencies]
tempfile = "3"
